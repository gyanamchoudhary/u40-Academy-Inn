# Production uses the Cloudflare D1 binding named DB from wrangler.jsonc.
# No database password or connection string is required.

# Public Turnstile site key injected by the frontend build environment.
# Use Cloudflare's documented test site key only for local development.
VITE_TURNSTILE_SITE_KEY=
